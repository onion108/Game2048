#include <stdio.h>

/*
游戏规则:
在4*4的界面内
一开始会出现两个数字，这两个数字有可能是2或者4，
每次可以选择上下左右其中一个方向去滑动，每一次滑动，
所有的数字方块都会往滑动的方向靠拢，
相同数字的方块在靠拢时会相加合并成一个，
不同的数字则靠拢堆放
最后在剩余的空白处生成一个数字2或者4。

一旦获得任意一个相加后的值为2048的数字，
则游戏成功。

同时数字2出现的概率相对4较大
*/





